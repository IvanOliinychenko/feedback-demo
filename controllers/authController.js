var {
  createError,
  BAD_REQUEST,
  CONFLICT,
  NOT_FOUND
} = require('../helpers/errorHelper');
var moment = require('moment');

var getToken = function(req, res, next) {
 
}


module.exports = {
  getToken
}